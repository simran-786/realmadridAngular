/*var jsonQuery = require('json-query')

var data = 'http://localhost:5000/realmadridAPI/players';

jsonQuery('team[TotalGoals=1].firstname', {
    data: data
})

$(document).ready(functional() {
    $.ajax({
        url: "http://localhost:5000/realmadridAPI/players"
    }).then(function(data, status, jqxhr) {
        $('.players-id').append(data.id);
        $('players-firstname').append(data.content);
        console.log(jqxhr);
    });
});

//To find players who are midfielders
database.RealMadrid.find({tags:{sin:["Midfielder"]}); 


*/